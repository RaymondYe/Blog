<br>
<div>
    <% if (!isFirstPage){%>
      <a href="?p=<%= page-1%>" title="上一页" class="btn btn-default">上一页</a>
    <% } %>
    <% if (!isLastPage){%>
      <a href="?p=<%= page+1%>" title="下一页" class="btn btn-default">下一页</a>
    <% } %>
</div>